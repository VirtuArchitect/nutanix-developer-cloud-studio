# v2.93.0 - Read-Only Adapter Authorization Gate

## Added

- Read-only adapter authorization gate records at `/api/prism/read-only-authorization-gates`.
- Authorization packet linking approved lab profile, successful fixture replay, and ready read-only lab gate evidence.
- Admin Providers panel for authorization checks.

## Guardrails

- Authorization is evidence-only.
- The gate does not enable future live calls by itself.
- `realPrismCallsEnabled=false` remains enforced.
