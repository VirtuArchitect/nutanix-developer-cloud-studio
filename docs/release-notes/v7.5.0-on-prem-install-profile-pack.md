# v7.5.0-on-prem-install-profile-pack

## Summary

Adds an on-prem install profile pack for durable operations planning.

## Highlights

- Adds `GET /api/onprem/install-profile-pack`.
- Adds profile templates under `docs/on-prem-profiles/`.
- Adds `validate:onprem-profile-pack`.
- Documents backup/restore validation commands and disabled-adapter guardrails.

## Safety

Profiles keep real Prism HTTP and real adapter mutation disabled.
