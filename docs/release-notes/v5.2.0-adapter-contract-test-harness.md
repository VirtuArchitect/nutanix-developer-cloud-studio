# v5.2.0 - Adapter Contract Test Harness

This release adds contract-harness records for future real adapter readiness.

## Added

- `GET /api/lab-transition/adapter-contract-harnesses`
- `POST /api/lab-transition/adapter-contract-harnesses`
- Admin Providers panel for contract harness evidence.
- Contract suites for Prism read-only shape, credential resolver behavior, timeout/retry behavior, redaction, and fail-closed behavior.

## Safety

- Harness records are non-executing evidence records.
- Live HTTP, provisioning, credential resolution, inventory import, and real Prism calls remain disabled.
