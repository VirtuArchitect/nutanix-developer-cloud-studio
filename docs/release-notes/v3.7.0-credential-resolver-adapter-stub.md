# v3.7.0 - Credential Resolver Adapter Stub

Added disabled credential resolver adapter stubs for future Vault, CyberArk, or environment-backed references.

## Added

- `GET /api/credentials/resolver-stubs`.
- `POST /api/credentials/resolver-stubs`.
- Mock contract tests for reference schemes, inherited redaction rules, and fail-closed behavior.

## Safety

- Resolver stubs never resolve, store, log, or export secret material.
