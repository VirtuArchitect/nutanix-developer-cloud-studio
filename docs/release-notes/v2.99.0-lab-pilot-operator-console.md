# v2.99.0 - Lab Pilot Operator Console

Added a consolidated operator console snapshot for the controlled read-only lab pilot.

## Added

- `GET /api/lab-pilot/operator-console`.
- Admin Operations panel for runtime mode, authorization gate, runbook workflow, inventory pilot, observability, evidence export, and readiness counters.

## Safety

- The console is read-only and does not mutate adapter mode, inventory, or infrastructure.
