# v7.1.0-migration-schema-baseline

## Summary

Adds schema versioning and non-destructive migration baseline evidence.

## Highlights

- Adds `GET /api/onprem/migration-baseline`.
- Adds `002_schema_version.sql` for schema version and signed audit manifest tables.
- Emits migration checksums and destructive-change detection.

## Safety

Migrations are additive and validation rejects destructive baseline patterns.
