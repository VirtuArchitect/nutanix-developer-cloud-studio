# v3.8.0 - Prism Read-Only HTTP Client Disabled Mode

Added the Prism Central read-only HTTP client shape while keeping execution disabled.

## Added

- `GET /api/prism/read-only-http-clients`.
- `POST /api/prism/read-only-http-clients`.
- Request-shape evidence for read-only inventory paths, timeout, retry, runtime flag, and authorization-gate requirements.

## Safety

- The HTTP client does not open network connections.
- Real Prism calls remain disabled.
