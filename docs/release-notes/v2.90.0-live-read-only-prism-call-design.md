# v2.90.0 - Live Read-Only Prism Call Design

## Added

- Design-only live read-only Prism Central call contract at `/api/prism/live-read-only-design`.
- Allowed endpoint list for clusters, projects, images, subnets, categories, and VMs.
- Timeout, retry, redaction, error taxonomy, and enablement gate records.
- Admin Providers panel for live read-only Prism call design review.

## Guardrails

- The endpoint is a design contract only.
- `realPrismCallsEnabled=false` remains enforced.
- No Prism Central network calls are executed.
