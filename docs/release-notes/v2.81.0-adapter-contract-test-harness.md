# v2.81.0 - Adapter Contract Test Harness

## Summary

Added a formal Prism adapter contract test harness for simulator and disabled real-adapter behavior.

## Added

- Direct tests for `MockPrismAdapter` health, inventory, planning, submit, poll, success, failed, timeout, and non-VM behavior.
- Direct tests for `DisabledRealPrismAdapter` fail-closed planning, submit, and poll behavior.
- Version metadata updated to `2.81.0-adapter-contract-test-harness`.

## Guardrails

- The real adapter remains disabled even when selected.
- Contract tests assert `provisioningEnabled=false`.
