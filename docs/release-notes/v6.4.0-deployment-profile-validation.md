# v6.4.0-deployment-profile-validation

## Summary

Adds fail-closed deployment profile validation.

## Highlights

- Adds `GET /api/deployment/profiles`.
- Validates `local`, `hosted-demo`, `on-prem-starter`, and `lab-prep` profiles.
- Makes durable state, trusted identity, and disabled real Prism HTTP posture explicit.

## Validation

- API and client helper tests cover the endpoint.
