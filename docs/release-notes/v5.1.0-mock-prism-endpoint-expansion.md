# v5.1.0 - Mock Prism Endpoint Expansion

This release records expanded mock Prism simulator behavior for future lab rehearsals.

## Added

- `GET /api/lab-transition/mock-prism-endpoint-expansions`
- `POST /api/lab-transition/mock-prism-endpoint-expansions`
- Admin Providers panel for simulator expansion evidence.
- Auth-shaped header contract, read-only endpoint inventory, latency simulation, rate-limit simulation, failure modes, and redacted request-log fields.

## Safety

- Expansion records describe local mock behavior only.
- Live HTTP, provisioning, credential resolution, inventory import, and real Prism calls remain disabled.
