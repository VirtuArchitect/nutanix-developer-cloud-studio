# v6.3.0-audit-integrity-manifest

## Summary

Adds tamper-evident audit manifest diagnostics.

## Highlights

- Adds `GET /api/audit/integrity-manifest`.
- Produces SHA-256 digests for retained audit event metadata.
- Produces a manifest digest without exporting credentials, authorization headers, or secret material.

## Validation

- API tests verify SHA-256 manifest shape and disabled real-infrastructure flags.
