# v7.0.0-durable-postgres-repository-foundation

## Summary

Adds durable persistence status and Postgres repository contract readiness.

## Highlights

- Adds `GET /api/onprem/durable-persistence`.
- Reports active repository mode, Postgres configuration status, migration directory, and fail-closed runtime posture.
- Keeps memory and JSON-file modes available.

## Safety

No PostgreSQL driver is added in this release. Postgres runtime remains fail-closed until dependency approval.
