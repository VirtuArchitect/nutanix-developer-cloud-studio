# v2.84.0 - Read-only Inventory Contract Tests

## Summary

Added fixture-driven contract tests for the read-only Prism inventory scaffold.

## Added

- Tests for deterministic cluster, project, image, network, category, and VM fixture imports.
- Tests for disabled read-only request plans and blocked mutation operations.
- API tests for blocked and ready read-only lab gate states.

## Guardrails

- Contract tests use fixtures only.
- No Prism Central network calls are made.
