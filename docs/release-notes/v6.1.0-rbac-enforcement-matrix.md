# v6.1.0-rbac-enforcement-matrix

## Summary

Adds an RBAC enforcement matrix for production hardening review.

## Highlights

- Adds `GET /api/security/rbac-matrix`.
- Lists protected hardening routes, allowed roles, denied roles, and `requireRole` enforcement.
- Emits denied-role negative test cases.

## Validation

- API tests verify Platform Admin access and Developer denial.
