# v6.0.0-api-contract-baseline

## Summary

Adds an OpenAPI-style API contract baseline snapshot for NDC Studio hardening review.

## Highlights

- Adds `GET /api/contracts/openapi`.
- Documents key API operations, required roles, request/response examples, and mutation boundaries.
- Keeps `provisioningEnabled=false` and `realPrismCallsEnabled=false`.

## Validation

- Covered by API and client helper tests.
